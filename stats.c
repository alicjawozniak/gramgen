#include "stats.h"

void statystyki_calosc(char* plik, int rzad){
    char* najczestsze[DLUGSLOWA];
    char* najrzadsze[DLUGSLOWA];
    char* temp [DLUGSLOWA];

    FILE* fp = fopen (plik, "r");

    while (fscanf(fp, "%s", temp) == 1){

    }

}

void statystyki_ngramu(char* plik, int rzad, char* prefiks){

}
